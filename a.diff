diff --git a/src/client/java/org/example2/playerrng/client/PlayerRngDiffTracker.java b/src/client/java/org/example2/playerrng/client/PlayerRngDiffTracker.java
index 1111111..2222222 100644
--- a/src/client/java/org/example2/playerrng/client/PlayerRngDiffTracker.java
+++ b/src/client/java/org/example2/playerrng/client/PlayerRngDiffTracker.java
@@ -1,6 +1,7 @@
 package org.example2.playerrng.client;

 import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.minecraft.client.MinecraftClient;
 import org.jetbrains.annotations.Nullable;

 public final class PlayerRngDiffTracker {
@@ -13,10 +14,10 @@ public final class PlayerRngDiffTracker {

     private PlayerRngDiffTracker() {}

-    public static void init() {
-        ClientTickEvents.END_CLIENT_TICK.register(PlayerRngDiffTracker::onEndTick);
-    }
-
     public static void markCause(String cause) {
         lastCause = cause;
     }
+
+    public static void init() {
+        ClientTickEvents.END_CLIENT_TICK.register(PlayerRngDiffTracker::onEndTick);
+    }

     private static void onEndTick(MinecraftClient client) {
         if (client.player == null) {
@@ -59,6 +60,14 @@ public final class PlayerRngDiffTracker {
         }
     }

+    public static void reset() {
+        currentSeed = null;
+        previousSeed = null;
+        lastDiffSteps = -1;
+        lastCause = "manual_reset";
+        changedThisTick = false;
+    }
+
     private static long nextSeed(long seed) {
         return (seed * 0x5DEECE66DL + 0xBL) & MASK_48;
     }
diff --git a/src/client/java/org/example2/playerrng/client/PlayerrngClient.java b/src/client/java/org/example2/playerrng/client/PlayerrngClient.java
index 3333333..4444444 100644
--- a/src/client/java/org/example2/playerrng/client/PlayerrngClient.java
+++ b/src/client/java/org/example2/playerrng/client/PlayerrngClient.java
@@ -1,10 +1,31 @@
 package org.example2.playerrng.client;

 import net.fabricmc.api.ClientModInitializer;
+import net.fabricmc.fabric.api.client.event.lifecycle.v1.ClientTickEvents;
+import net.fabricmc.fabric.api.client.keybinding.v1.KeyBindingHelper;
+import net.minecraft.client.option.KeyBinding;
+import net.minecraft.client.util.InputUtil;
+import org.lwjgl.glfw.GLFW;

 public class PlayerrngClient implements ClientModInitializer {
+    private static KeyBinding resetKey;
+
     @Override
     public void onInitializeClient() {
         PlayerRngDiffTracker.init();
         PlayerRngHudOverlay.init();
+
+        resetKey = KeyBindingHelper.registerKeyBinding(new KeyBinding(
+                "key.playerrng.reset_tracker",
+                InputUtil.Type.KEYSYM,
+                GLFW.GLFW_KEY_F8,
+                "category.playerrng"
+        ));
+
+        ClientTickEvents.END_CLIENT_TICK.register(client -> {
+            while (resetKey.wasPressed()) {
+                PlayerRngDiffTracker.reset();
+                if (client.player != null) {
+                    client.player.sendMessage(net.minecraft.text.Text.literal("[playerrng] tracker reset"), true);
+                }
+            }
+        });
     }
 }